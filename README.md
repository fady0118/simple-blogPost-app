### LearnWebCode  Backend Course Project

A minimal **Node.js + Express** blog example from the LearnWebCode backend course. Uses **EJS** for views and **better-sqlite3** for a small, fast SQLite-backed data store. Includes a simple **CRUD** flow for posts and basic authentication views.
